description = "Show the Gemini tm/Linear setup path for this branch"

prompt = """
Explain the supported Gemini CLI tm/Linear setup path for this branch.

Required points:
1. Preferred path: run `./scripts/install.sh --gemini` from a Hyperpowers checkout
2. This installs/links the Gemini extension and provisions the shared tm runtime
3. Configure `LINEAR_API_KEY` and `LINEAR_TEAM_KEY`
4. Verify with `~/.local/bin/tm --help` and `tm sync`
5. Gemini users get a tm-oriented task surface through the installed extension

{{args}}
"""
